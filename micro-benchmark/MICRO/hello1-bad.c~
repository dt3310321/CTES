int  buf1[10];
void test(){
   char s[10]; //stack
   char s1[20];
   int *buf2=(int*)malloc(10*sizeof(int));//heap
   char * s2=(char*)malloc(20*sizeof(char));
   int i=10;
   int j=i;
   int k=10; //1读操作
   
    
//end
 int *p=buf1;
   p=p+10;
   *p=100;  //14简单指针
   *(p+10)=200; //15指针运算
s[buf1[0]]='a';//21
//s[*p]='c';//22
   free(buf2);
   free(s2);
  return 0;
}

